<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>로그인 - CODE CIRCLE</title>
<style>
    /* === 1. 기존 프로젝트의 CSS 변수를 그대로 사용하여 디자인 일관성 유지 === */
    :root {
        --color-canvas-default: #0d1117;
        --color-canvas-subtle: #161b22;
        --color-border-default: #30363d;
        --color-fg-default: #e6edf3;
        --color-fg-muted: #7d8590;
        --color-accent-fg: #1f6feb;
        --color-accent-emphasis: #238636;
        --color-accent-emphasis-hover: #2ea043;
        --color-fg-on-emphasis: #ffffff;
        --border-radius-medium: 6px;
    }

    /* === 2. 기본 페이지 스타일 === */
    body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background-color: var(--color-canvas-default);
        color: var(--color-fg-default);
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
    }

    /* === 3. 로그인 폼을 감싸는 박스 === */
    .login-box {
        background-color: var(--color-canvas-subtle);
        border: 1px solid var(--color-border-default);
        border-radius: var(--border-radius-medium);
        padding: 2.5rem;
        width: 100%;
        max-width: 380px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
    }

    .login-box h3 {
        font-size: 24px;
        font-weight: 600;
        text-align: center;
        margin-top: 0;
        margin-bottom: 1.5rem;
    }

    /* === 4. 폼 요소 스타일 === */
    .form-group {
        margin-bottom: 1rem;
        text-align: left;
    }

    .form-group label {
        display: block;
        font-size: 14px;
        font-weight: 500;
        margin-bottom: 0.5rem;
    }

    .form-group input {
        width: 100%;
        padding: 10px 12px;
        background: var(--color-canvas-default);
        border: 1px solid var(--color-border-default);
        border-radius: var(--border-radius-medium);
        color: var(--color-fg-default);
        font-size: 14px;
        box-sizing: border-box;
        transition: all 0.2s ease-in-out;
    }

    .form-group input:focus {
        outline: none;
        border-color: var(--color-accent-fg);
        box-shadow: 0 0 0 3px rgba(31, 111, 235, 0.3);
    }
    
    /* === 5. 버튼 스타일 === */
    .btn {
        display: block; /* a 태그도 버튼처럼 보이게 하기 위해 block으로 설정 */
        width: 100%;
        padding: 10px 16px;
        font-size: 14px;
        font-weight: 600;
        text-align: center;
        text-decoration: none;
        cursor: pointer;
        border: 1px solid var(--color-border-default);
        border-radius: var(--border-radius-medium);
        transition: background-color 0.2s;
        box-sizing: border-box;
    }

    .btn--primary {
        background-color: var(--color-accent-emphasis);
        color: var(--color-fg-on-emphasis);
        border-color: var(--color-accent-emphasis);
    }
    
    .btn--primary:hover {
        background-color: var(--color-accent-emphasis-hover);
    }

    /* [추가됨] 기본 버튼 스타일 (얼굴 로그인 버튼용) */
    .btn--secondary {
        background-color: var(--color-border-muted);
        color: var(--color-fg-default);
    }
    
    .btn--secondary:hover {
        background-color: var(--color-border-default);
    }
    
    /* [추가됨] 버튼들을 그룹으로 묶어 간격을 주기 위함 */
    .button-group {
        display: flex;
        flex-direction: column; /* 버튼을 세로로 나열 */
        gap: 0.75rem; /* 버튼 사이의 간격 */
        margin-top: 1rem; /* 폼과 버튼 그룹 사이의 간격 */
    }
    
    /* === 6. 회원가입 링크 영역 === */
    .signup-link {
        margin-top: 1.5rem;
        padding: 1rem;
        border: 1px solid var(--color-border-default);
        border-radius: var(--border-radius-medium);
        text-align: center;
        font-size: 14px;
    }

    .signup-link a {
        color: var(--color-accent-fg);
        font-weight: 600;
    }

</style>
</head>
<body>

	<div class="login-box">
		<h3>로그인</h3>

		<form action="/user/login" method="POST">
			<div class="form-group">
				<label for="id">아이디</label>
				<input type="text" name="id" id="id" required>
			</div>
			
			<div class="form-group">
				<label for="password">비밀번호</label>
				<input type="password" name="password" id="password" required>
			</div>
			
			<div class="button-group">
				<button type="submit" class="btn btn--primary">로그인</button>
				<a href="/user/face-login" class="btn btn--secondary">얼굴로 로그인</a>
			</div>
		</form>
		
		<div class="signup-link">
			<span>계정이 없으신가요?</span>
			<a href="/user/signup">회원가입</a>
		</div>
	</div>

</body>
</html>