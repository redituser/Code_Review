<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>CODE CIRCLE - 인기 게시글</title>
    <style>
        body { background-color: #0d1117; color: #e6edf3; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }
        a { color: #1f6feb; text-decoration: none; }
        a:hover { text-decoration: underline; }
        .container { max-width: 900px; margin: 20px auto; padding: 20px; }
        .page-header { display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid #30363d; padding-bottom: 12px; margin-bottom: 12px; }
        .btn { border: 1px solid #30363d; background-color: #21262d; color: #e6edf3; padding: 6px 12px; border-radius: 6px; cursor: pointer; }
        .btn:hover { background-color: #30363d; }
        .post-list-item { display: flex; gap: 12px; padding: 16px 0; border-bottom: 1px solid #21262d; }
        .post-list-content { flex: 1; }
        .post-list-title { font-weight: 600; font-size: 18px; margin-bottom: 8px; display: block; }
        .post-list-meta { color: #7d8590; font-size: 12px; }
        .pagination { text-align: center; margin-top: 30px; }
        .pagination a, .pagination span { margin: 0 5px; color: #e6edf3; padding: 8px 14px; border: 1px solid #30363d; border-radius: 6px; }
        .pagination a:hover { background-color: #30363d; }
        .pagination .current { background-color: #1f6feb; border-color: #1f6feb; color: white; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="page-header">
            <h1>인기 게시글</h1>
            <a href="/" class="btn">메인으로</a>
        </div>

        <div class="post-list">
            {{#boardPage.content}}
            <article class="post-list-item">
                <div class="post-list-content">
                    <a href="/board/detail/{{boardNo}}" class="post-list-title">{{boardTitle}}</a>
                    <div class="post-list-meta">
                        <span>작성자: {{writerId}}</span> |
                        <span>작성일: {{createDate}}</span> |
                        <span style="color: #f85149;">❤️ {{likeCount}}</span>
                    </div>
                </div>
            </article>
            {{/boardPage.content}}
            {{^boardPage.content}}
            <p style="text-align: center; padding: 50px; color: #7d8590;">아직 게시글이 없습니다.</p>
            {{/boardPage.content}}
        </div>

        <div class="pagination">
            {{^boardPage.first}}
                <a href="/board/popular?page={{previousPage}}">&laquo; 이전</a>
            {{/boardPage.first}}

            <span class="current">{{currentPage}}</span>

            {{^boardPage.last}}
                <a href="/board/popular?page={{nextPage}}">다음 &raquo;</a>
            {{/boardPage.last}}
        </div>
    </div>
</body>
</html>